================================================================================
INTERMEDIATE OUTPUTS - QUICK REFERENCE CARD
================================================================================

🚀 QUICK START (30 seconds)
--------------------------------------------------------------------------------
from vllm import LLM, SamplingParams

llm = LLM("Qwen/Qwen3-VL-8B-Instruct")
params = SamplingParams(output_hidden_states=True, output_logits=True)
outputs = llm.generate("What is AI?", params)

hidden = outputs[0].outputs[0].hidden_states  # [tokens, 4096]
logits = outputs[0].outputs[0].logits         # [tokens, vocab_size]

================================================================================
📚 DOCUMENTATION
================================================================================
QUICKSTART:           INTERMEDIATE_OUTPUTS_QUICKSTART.md
FULL DOCS:            examples/INTERMEDIATE_OUTPUTS_README.md
IMPLEMENTATION:       IMPLEMENTATION_SUMMARY.md
TESTING:              TESTING_CHECKLIST.md
BENCHMARKS:           benchmarks/BENCHMARK_README.md

================================================================================
🔧 API REFERENCE
================================================================================

SamplingParams:
  output_hidden_states = False | True | "final" | "all"
  output_logits        = False | True
  output_attention_weights = False | True  (future)

CompletionOutput:
  .hidden_states       → torch.Tensor | None
  .logits             → torch.Tensor | None
  .all_hidden_states  → dict[int, torch.Tensor] | None  (future)
  .attention_weights  → dict[int, torch.Tensor] | None  (future)

================================================================================
📝 EXAMPLES
================================================================================
SIMPLE:               python3 examples/qwen3_vl_simple.py
ALL EXAMPLES:         python3 examples/qwen3_vl_intermediate_outputs.py
SPECIFIC:             python3 examples/qwen3_vl_intermediate_outputs.py [1-5]

================================================================================
⚡ PERFORMANCE TESTS
================================================================================
QUICK (30s):          python3 benchmarks/quick_perf_test.py
QWEN3-VL:             python3 benchmarks/qwen3_vl_perf_test.py
COMPREHENSIVE:        python3 benchmarks/benchmark_intermediate_outputs.py

================================================================================
📊 EXPECTED PERFORMANCE
================================================================================
Configuration         Time Overhead    Memory/50 tokens
--------------------------------------------------------------------------------
Disabled (default)    0%               0
Hidden states only    2-5%             ~800 KB
Logits only           5-10%            ~30 MB
Both                  10-15%           ~31 MB

Note: All tensors stored on CPU, minimal GPU memory impact

================================================================================
✅ TESTING
================================================================================
UNIT TESTS:           pytest tests/v1/sample/test_sampling_params_e2e.py
SYNTAX CHECK:         python3 -m py_compile vllm/*.py  ✓ DONE
INTEGRATION:          See TESTING_CHECKLIST.md

================================================================================
💡 COMMON USE CASES
================================================================================

1. REPRESENTATION ANALYSIS
   params = SamplingParams(output_hidden_states=True)
   hidden = outputs[0].outputs[0].hidden_states
   # Cluster, visualize, analyze

2. PROBABILITY ANALYSIS
   params = SamplingParams(output_logits=True)
   logits = outputs[0].outputs[0].logits
   probs = torch.softmax(logits, dim=-1)

3. BATCH WITH MIXED CONFIGS
   params_list = [
       SamplingParams(output_hidden_states=True),
       SamplingParams(output_logits=True),
       SamplingParams(output_hidden_states=True, output_logits=True),
   ]
   outputs = llm.generate(prompts, sampling_params=params_list)

4. WITH VISION
   prompt = "<|im_start|>system\n...<|vision_start|><|image_pad|><|vision_end|>..."
   outputs = llm.generate(
       {"prompt": prompt, "multi_modal_data": {"image": image}},
       params
   )

5. SAVE FOR LATER
   torch.save(output.hidden_states, "hidden.pt")
   torch.save(output.logits, "logits.pt")

================================================================================
🐛 TROUBLESHOOTING
================================================================================

GPU OOM:
  - Reduce batch_size
  - Reduce max_tokens
  - Use tensor_parallel_size
  - Disable logits (they're large)

No intermediate outputs:
  - Check output_hidden_states=True in SamplingParams
  - Verify using correct branch/version

Slow performance:
  - Only enable outputs you need
  - Use smaller batch sizes
  - Check GPU utilization

================================================================================
📦 FILES MODIFIED
================================================================================
Core (7 files):
  vllm/sampling_params.py
  vllm/outputs.py
  vllm/v1/outputs.py
  vllm/v1/worker/gpu_model_runner.py
  vllm/v1/core/sched/scheduler.py
  vllm/v1/engine/__init__.py
  vllm/v1/engine/output_processor.py

Tests (1 file):
  tests/v1/sample/test_sampling_params_e2e.py

Examples (2 files):
  examples/qwen3_vl_simple.py
  examples/qwen3_vl_intermediate_outputs.py

Benchmarks (3 files):
  benchmarks/quick_perf_test.py
  benchmarks/qwen3_vl_perf_test.py
  benchmarks/benchmark_intermediate_outputs.py

Docs (5 files):
  INTERMEDIATE_OUTPUTS_QUICKSTART.md
  examples/INTERMEDIATE_OUTPUTS_README.md
  benchmarks/BENCHMARK_README.md
  IMPLEMENTATION_SUMMARY.md
  TESTING_CHECKLIST.md

================================================================================
🎯 DESIGN HIGHLIGHTS
================================================================================
✓ Backward compatible (disabled by default)
✓ Zero overhead when disabled
✓ Per-request configuration
✓ CPU storage (minimal GPU impact)
✓ Async transfer (non-blocking)
✓ Extensible for future features

================================================================================
🔮 FUTURE PHASES
================================================================================
Phase 2: All layer hidden states (output_hidden_states="all")
Phase 3: Attention weights (output_attention_weights=True)
Phase 4: Streaming mode optimization
Phase 5: Model-specific implementations

================================================================================
STATUS: ✅ PHASE 1 COMPLETE - READY FOR TESTING
================================================================================
Implementation Date: 2024-02-24
All code syntax-validated and ready to run
Dependencies: torch, vllm, transformers, pillow

Next: Install dependencies and run tests (see TESTING_CHECKLIST.md)
================================================================================
